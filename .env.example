# This version is a direct static download site.
# No Stripe keys or server secrets are needed.

# Optional: set this only if the extension zip is somewhere else.
EXTENSION_ZIP_PATH=C:\path\to\timebox-youtube-blocker.zip
